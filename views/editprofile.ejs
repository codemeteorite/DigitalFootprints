<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Profile</title>

  <script src="https://cdn.tailwindcss.com"></script>

  <script>
    tailwind.config = { darkMode: "class" };
  </script>
</head>

<body class="bg-gray-100 dark:bg-gray-900 p-6 min-h-screen text-gray-900 dark:text-gray-100">

  <div class="max-w-lg mx-auto bg-white dark:bg-gray-800 p-8 rounded-2xl shadow-xl">

    <h1 class="text-2xl font-bold mb-6 text-center">Edit Profile</h1>

    <form action="/edit-profile" method="POST" class="space-y-5">

      <!-- USERNAME -->
      <div>
        <label class="block mb-1 font-medium">Username</label>
        <input type="text" name="username" value="<%= user.username %>"
          class="w-full p-3 rounded-xl bg-gray-200 dark:bg-gray-700 focus:ring-2 focus:ring-indigo-500 outline-none">
      </div>

      <!-- PHOTO URL -->
      <div>
        <label class="block mb-1 font-medium">Profile Photo URL</label>
        <input type="text" name="photo_url" value="<%= user.profile_photo_url || '' %>"
          class="w-full p-3 rounded-xl bg-gray-200 dark:bg-gray-700 focus:ring-2 focus:ring-indigo-500 outline-none">
      </div>

      <% if (user.profile_photo_url) { %>
        <div class="text-center">
          <img src="<%= user.profile_photo_url %>" class="w-32 h-32 rounded-full object-cover mx-auto shadow-md">
        </div>
      <% } %>

      <button
        class="w-full bg-indigo-600 hover:bg-indigo-700 text-white p-3 rounded-xl font-semibold transition">
        I'm Looking Great
      </button>
    </form>

    <div class="text-center mt-4">
      <a href="/profile"
        class="text-indigo-500 hover:underline">Back  to Profile?</a>
    </div>

  </div>

</body>

</html>
